import StrategyHero from "../strategies/StrategyHero";
import StrategyLeaderboard from "../strategies/StrategyLeaderboard";
import StrategyContribution from "../strategies/StrategyContribution";
import StrategyInsightsPanel from "../strategies/StrategyInsightsPanel";

export default function StrategiesWorkspace() {
  return (
    <div className="flex flex-col h-full p-6 gap-4 overflow-y-auto bg-[hsl(var(--bg-2))]">
      
      {/* PASS 9: HERO — Strategy Performance */}
      <StrategyHero />

      {/* PASS 9: Core Grid */}
      <div className="grid grid-cols-3 gap-4">
        
        {/* LEFT: Leaderboard + Contribution */}
        <div className="col-span-2 flex flex-col gap-4">
          <StrategyLeaderboard />
          <StrategyContribution />
        </div>

        {/* RIGHT: Insights Panel */}
        <StrategyInsightsPanel />
      </div>

    </div>
  );
}
